<p>
  By <strong><%= comment.author.name %></strong>
</p>

<p>
  <%= comment.body %>
</p>

<p>
  <% if comment.author == current_user || comment.author.admin? %>
    <%= link_to 'Delete comment', [comment.post, comment],
                method: :delete,
                data: { confirm: "Are you sure?" },
                class: 'btn btn-outline-dark'%>
  <% end %>
  <hr>
</p>
